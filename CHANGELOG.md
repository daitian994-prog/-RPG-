# 更新记录

## 0.6.0 — 2026-08-21

- 将“地图 / 行程”从可缩放大地图和标点操作，完整重构为适合手机滚动浏览的地点卡片列表，保留帕拉斯、断风森林、战争遗迹和山间寺庙四个地点。
- 每张卡片拆分为 240px 固定图片区、地点信息区、独立线索区和行动区；新增或展开线索只会改变文字区高度，不会拉伸图片。
- 地点卡统一显示名称、氛围副标题、风险、玩法标签、可能获得的反馈和当前位置，并接入项目已有的艾欧尼亚场景素材。
- 线索区默认只展示一条主线索，多条线索可按需展开；点击卡片会优先沿主线索前往，同时保留明确的“不追线索，自由探索”入口。
- 保留第一章固定终章节奏和帕拉斯恢复行动，通过后端 105 项回归测试与前端生产构建。

## 0.5.1 — 2026-08-21

- 修复 Windows 一键启动器优先选择 Codex 自带 Python、忽略项目 `.venv`，导致已安装 FastAPI 的本地环境仍无法启动的问题；现在优先使用项目虚拟环境。
- 修复依赖探测产生标准错误输出时被 PowerShell 的停止策略提前中断、无法进入自动安装流程的问题；依赖检查现在依据退出码决定是否安装。
- 实际执行无浏览器启动、`/health` 版本检查与安全停止，确认本地服务器可以正常启动且存档不受影响。

## 0.5.0 — 2026-08-21

- 新增帕拉斯短开场：角色创建后先接触两条自然世界信号，并稳定获得“断风森林出现非野兽脚印”的可追踪信息，再第一次打开地图；内容保留多种安全变体，不采用传统任务清单。
- 复用旅途日志保存轻量可追踪事项，支持相关地点、来源、状态、NEW 标记与主动关注；日志按“正在关注、当前可追踪、新发现”展示，不记录完成度或任务完成。
- 四个地点新增稳定玩法人格、风险预期和常见反馈类别；地点人格进入事件导演概率修正，地图同时保留自由探索入口，具体 Scene 仍由 AI 或安全保底动态创造。
- 地图追踪选择新增 `playerIntent`，与相关 Lead、玩家关注、地点人格一起进入 `EventContext`；Director 强化相关 WorldThread 权重，AI 和离线保底都必须优先回应玩家为何来到这里。
- AI Scene 结果可提出 `suggestedLead`，程序会验证地点、现有 WorldThread 与当前 Scene 关联后才写入；Clue 同时承担场景知识、语义相关检定优势和后续地点引导。
- 跑通首条完整反馈链：帕拉斯脚印传闻 → 断风森林追踪 Scene → 确认穿靴者活动 → 新 Clue → 战争遗迹出现“旧军道近期有人活动”NEW 方向；结算页新增“你确认了一件事 / 新的方向”。
- 修复动态战斗拥有四个行动时战斗结算文本可能越界，以及失败文本分支不可达的问题；新增反馈循环专项测试，后端 105 项测试与前端生产构建通过。

## 0.4.2 — 2026-08-21

- Scene 每轮结算后立即清空旧行动，并把最新 Facts、Questions、Actors、Objects、上一行动、检定结果、AIResult、历史行动与当前焦点重新交给行动生成器；下一轮选项不再复用首轮内容。
- 新增最小行动去重：比较行动标签、目标与规范化语义；无理由重复会被拒绝并要求 AI 重写，只有新事实形成了新的明确目标时才允许同类行动再次出现。
- AIResult 新增结构化 `SceneDecision`，逐轮返回 `continueScene`、仅供调试的 `reason` 与下一轮 `nextFocus`；Scene 是否结束改由当前是否仍有必须立即处理且能产生新决策的问题决定，不再以未达到第 4 轮为默认继续条件。
- `maxRounds = 4` 仅保留为安全上限；新增连续两轮事实、问题、焦点和行动均停滞时的防循环收束，AI 同时被要求依据已有事实自然结束，不得凭空加入陌生人物、组织、神器、敌人、英雄或世界危机拖延事件。
- 玩家选项移除“目标/可能代价”等后台工程说明，只保留自然行动文本、能力、成功率与风险；开发视图新增 Previous Actions、Current Focus、Scene Decision、结束原因及新一轮 Actions。
- 新增 10 个 Scene 的 1/2/3/4 轮差异化收束测试，以及“无法忽略的痕迹”三轮闭环、旧行动失效、焦点迁移、行动去重、最大轮数与防循环回归；后端 102 项测试与前端生产构建通过。

## 0.4.1 — 2026-08-21

- 将普通动态事件的 `pendingEvent` 升级为可持续 `SceneState`，保存现场人物、物件、已知事实、未解问题、上一轮行动与结果；事件可以自然持续最多 4 轮，也可以因明确离开、主要问题解决或失去行动能力在一轮结束。
- AI 在每轮检定后根据真实 Scene Facts、玩家行动和 Check Tier 生成结构化 `AIResult`，具体写出新增事实、新问题、已解决问题与 NPC 反应；`OutcomeEngine` 会验证结果档位、既有事实、世界线程边界、数值、物品和受保护角色，失败时重试一次并回退到本地具体结果。
- 下一轮行动改为根据更新后的 SceneState 重新生成，再由程序映射能力、难度与成功率；移除 AI 行动随机继承旧保底选项奖励、线索、状态或关系包的逻辑。
- 为线索加入能力、线程、目标、行动、地点与阵营相关性；每次检定只取相关性最高的两条，重复线索只保留最高加成，无关线索不再参与检定。物品情境加成同样要求行动或目标匹配。
- 前端在 Scene 未结束时直接展示本轮具体后果和下一轮行动，不再提前进入完整结算；DEV Debug 可查看 Scene ID、轮次、事实、问题、上一行动、Check、AIResult、验证结果与继续状态。
- 停用高频万能结算套话，并新增铜钟两轮现场、一轮离开、AI 行动机械包隔离、AIResult 越权验证和 Clue 相关性/去重/最多两条等回归测试；后端 96 项测试与前端生产构建通过。

## 0.4.0 — 2026-08-17

- 将第一章试玩扩展为一年四季、每季 4 次行动，共 16 次行动；前 12 次保留自由探索，最后 4 次进入不可跳过的固定终章。
- 新增“暮钟与无名之树”“风留下的名字”“血旗到来之前”三幕收束，依次为灵体异常、亚索同行与诺克萨斯残军建立本章局部结论及后续伏笔。
- 玩家在三幕中的选择会真实写入世界线结果、亚索的认可/信任/尊重与帕拉斯守备强度，守备选择会直接降低最终 Boss 检定难度。
- 为两条世界线程加入第一章节奏上限，防止高紧迫度线程在试玩中段提前自然结束；终章开始后关闭休息、自由调查与线程干预。
- Boss 战后停在第一年冬末，生成三线结局总结、玩家本章称号和第二章引子，并锁定后续行动；新增“重新开始试玩”入口。
- 修复固定终章事件仍尝试远程改写而可能长期锁住选项的问题，章节正文现在直接使用已审定文本并可靠解锁选择。
- 完成旧存档向 16 行动时间轴的迁移，并补充完整流程、结局状态与前端正式构建验证。

## 0.3.18 — 2026-08-17

- 将本项目 Git 提交身份固定为仓库所有者 `daitian994-prog` 的 GitHub 隐私邮箱，使 Vercel 能在私人仓库中识别项目所有者并自动触发部署。
- 一键发布程序会在每次发布前自动校正项目级 Git 身份，避免后续 Codex 或其他本地工具再次写入无法被 Vercel 识别的提交作者。

## 0.3.17 — 2026-08-17

- 修复一键发布入口在旧版 Windows 命令行中的中文乱码，将实际启动流程改为纯 ASCII 兼容入口。
- 项目没有新修改时不再抛出红色异常，改为明确提示“已经全部发布”；发布结束后窗口保留结果并等待用户关闭。

## 0.3.16 — 2026-08-17

- 新增面向代码小白的“一键发布到 GitHub 和 Vercel”入口，自动提升补丁版本、同步版本文件、写入更新日志、提交标签并生成离线 bundle 备份。
- 发布前自动运行全部后端测试、前端正式构建和 Git 文件检查；发现密钥、数据库、日志、依赖或缓存文件时会停止上传。
- 发布成功后自动推送 GitHub 主分支与版本标签，由 Vercel 接收提交并触发线上部署；原“创建新版本”入口继续兼容同一流程。

## 0.3.15 — 2026-08-17

- 修复“无名者”背景身份被错误生成为主动事件能力的问题；身份标签与可发动特质正式分层，只有明确声明 `usableInEvents` 且配置事件行动的特质才能进入动态选项。
- 重写 AI 不可用时的本地事件保底：按地点气味、声音、现场人物动作、具体物件、矛盾与迫近风险生成 4 段、约 300—450 字的完整事件，不再退化为一句机械骨架。
- 结构化 AI Scene 现在直接作为最终事件正文，不再把 `playerObservableFacts` 和 `immediateProblem` 重复追加，也不再进行第二次事件润色请求，减少延迟、费用和双重失败概率。
- 新增正文质量闸门：不足 220 字、少于 4 段、段落重复或违反规则边界的 AI 场景会被拒绝；短文本会自动要求 AI 修订一次，仍过短时保留 AI 创造的人物、物件和行动并由本地扩展为完整四段现场，其他越权内容切换到安全保底。
- 恢复情境化结算后果：调查、交涉、机动、战斗、体魄、心志和无需检定行动分别生成与现场人物、物件、目标和结果档位对应的具体变化，移除“按照已经确定的结果回应”等后台占位语言。
- 改进 AI 节点诊断：区分连接失败、超时、HTTP 状态错误、网络传输错误和响应格式错误；游戏服务产生的安全诊断可由管理器状态栏读取，不再出现节点显示正常而游戏持续回退的假象。
- 实际诊断确认当前已配置节点发生 `ConnectError`；即使该网络问题继续存在，完整事件和结算现在仍可由本地叙事稳定呈现。
- 新增身份标签隔离、离线正文长度、四段结构、AI 短文修订扩展、事实去重、后台占位语清理、单次事件 AI 调用与共享错误诊断测试；后端共 92 项测试通过。

## 0.3.14 — 2026-08-17

- 完成阶段5叙事权限重构：程序继续独占时间、线程、导演、检定、随机数、结果档位、奖励、伤势与状态写回，AI 改为在 Narrative Envelope 内真正创造具体现场、普通 NPC、物件、对白与行动提案。
- 新增结构化 AI Scene 与行动语义验证；AI 先提出 2—5 个自然行动，程序拒绝越权或重复内容，再映射六项能力、是否检定、难度与最终 2—4 个玩家选项；AI 不可决定成功率或结果。
- 结算叙事新增 Required Outcome 约束，模型只能表现程序已经确定的大成功、成功、部分成功或失败及其代价，远程 AI 不可改写机械结果。
- 新增亚索 Hero Actor V1：拥有持续位置、目标进度、行动点、可用状态、四维玩家关系、重要记忆与受路线约束的自主行动；世界线程推进后先执行英雄 Tick，再交由事件导演选取事件。
- 亚索相逢分为无、痕迹、传闻、远望、互动、合作六级，权重读取地点、目标、线程、可用状态、关系、近期出场、导演预算与稳定随机因子；离开艾欧尼亚东部时权重归零。
- 第一章亚索终局相逢支持初见、已认识和记得重要共同经历三种状态；亚索可以调查或延缓诺克萨斯危机，但不能替玩家解决世界线程或关键决定。
- 开发视图新增叙事信封、AI 场景/行动提案、拒绝原因、亚索 Runtime、行动日志与事件权重；生产玩家接口继续剥离全部内部状态。
- 新增阶段5专项测试：30次同阶段场景变化、行动映射与越权回退、20次英雄 Tick、六级相逢、危机不可自动解决、重要记忆回调及第一章三种相逢状态；后端共 86 项测试通过。

## 0.3.13 — 2026-08-17

- 修复事件正文仍会出现“事件自行运转”“并非等待领取的任务”等后台设计说明的问题，普通事件、英雄相逢与导演引导句全部改为世界内的现场描写。
- 清理人物页“世界不会等待玩家”“不是强制任务”等界面元叙事，并将世界线程介入反馈改为角色能够理解的自然表述。
- 新增跨四地点叙事文本防泄漏测试，阻止后台设计术语再次进入玩家正文。

## 0.3.12 — 2026-08-17

- 清理玩家前台的事件导演、现场组件、权重、线程阶段、随机数、Seed 与内部状态链等开发信息；本地开发仅可通过 `?debug=1` 显式开启统一开发视图，生产构建无入口。
- 新增玩家 API 投影层：生产默认仅返回已知世界动态、世界观物品/线索和可理解的检定信息，不再下发完整内部状态、候选池及叙事校验记录。
- 动态事件选项改为行动语义优先：先生成目标、手段、风险和可能结果，再映射六项核心能力，不再根据人物强弱属性套三条通用文案。
- 动态事件每次提供 2—4 条策略不同的行动，支持观察、记录、撤离等无需检定选项，并可按已持有线索与人物特质生成条件行动。
- 新增行动候选验证与开发记录，包含拒绝原因、能力映射理由和重复策略组；AI 叙事仍只能润色程序已经决定的行动语义。
- 完成 50 次动态事件质量模拟，覆盖 2/3/4 选项、六种能力、无需检定比例与单事件重复检查；后端 78 项测试和前端生产构建通过。

## 0.3.11 — 2026-08-17

- 将“人物故事”从独立知识库分类入口并入“英雄与人物故事”，英雄数量与已关联故事数量在同一标签中显示。
- 数据库继续以规范化方式单独保存故事正文，英雄列表接口在读取时按 `story_ids` 与 `related_characters` 聚合故事摘要，避免多人关联故事被重复复制。
- 英雄卡片同时展示人物传记与全部关联故事标题、作者和阅读时长；可在人物卡片内新增故事，点击故事可直接阅读正文并进入故事资料编辑。
- 英雄搜索同时覆盖人物传记与关联故事内容，并新增单条知识记录读取接口。

## 0.3.10 — 2026-08-17

- 后端数据管理器新增“项目总览与日志”独立功能页，集中展示当前版本、已开发系统、技术结构、网页端对接约定与完整版本更新记录。
- 新增 `GET /api/admin/project-status` 结构化状态接口，并提供可一键复制的 Markdown 对接文档与 JSON，网页端无需依赖口头描述判断开发进度。
- 新增结构化项目状态源，版本号实时读取 `VERSION`、更新记录实时解析 `CHANGELOG.md`，减少多个页面各自硬编码造成的信息漂移。
- 修复 API 节点管理器被旧版游戏服务占用 8000 端口后无法打开的问题；启动器只会自动替换 PID、启动时间和可执行文件均吻合的本项目旧服务，陌生进程仍安全拒绝。
- 清理 README 中“固定20个事件”和已删除旧属性的残留描述，统一为动态事件、六项核心能力、伤势与完整闭环的当前实现。

## 0.3.9 — 2026-08-17

- 将 pnpm 锁文件提升到仓库根目录并声明标准 workspace，使 Vercel 能在安装阶段正确识别锁文件。
- 在根 `package.json` 固定 Vercel 支持的 pnpm 10.28.1，并统一根目录安装与构建命令。

## 0.3.8 — 2026-08-17

- 新增 Vercel Python Function 入口、Vite 构建与 SPA/API rewrite 配置，项目根目录可直接导入 Vercel。
- 本地存档继续使用 SQLite；Vercel 生产环境改用官方 Runtime Cache 保存每位试玩者的 JSON 状态，避免向只读函数文件系统写入持久存档。
- 生产环境不再暴露本地 API 节点管理与知识库编辑后台，DeepSeek 密钥只从服务端环境变量读取。
- 将游戏地图素材交由 Vite 打包，移除公开试玩对 `/admin` 静态目录的依赖，并补充移动端安全区、缩放与触控尺寸兼容。
- 完善 `.env.example`、`.gitignore`、`.vercelignore`、Python 版本与根依赖配置，并新增 Vercel 存储适配测试。

## 0.3.7 — 2026-08-17

- 删除常规流程对 `e01`—`e20` 固定事件及固定 `eventProfiles`、`threadBindings` 映射的全部依赖，原 `events.json` 不再存在。
- 新增程序化 Dynamic Event Generator，每次行动根据地点、世界时间、人物能力/伤势、线程阶段/紧迫度/认知、玩家关注与Director张力即时建立候选池。
- 将场景、参与者、关键物、现场压力、事件意图和六种能力路线拆为独立组件；每个候选的标题、现场事实、三条选项、难度、结果与线索均由程序组合。
- 世界线程事件与英雄交叉不再套用旧事件正文，而是使用当前Thread Stage或英雄当前目标重新生成专属现场；英雄仍受低频Narrative Budget约束。
- 动态事件ID、组件组合和Director抽取均使用状态Seed，同一快照可复现，世界时间或人物/线程状态改变后会生成新的机械事件。
- 事件页标明“动态生成”并显示本次使用的现场组件；完整Debug保留组成项、权重、检定与结果写回。
- 40次跨地点模拟共生成480个事件、427种组件组合，实际抽取40个不同机械事件，覆盖四类事件与10种意图。

## 0.3.6 — 2026-08-17

- 新增统一 `EventContext`，将地点、时间、玩家状态、线索、活跃世界线程、导演意图、候选事件、硬事实与禁止改写项作为唯一叙事输入协议。
- 新增严格 AI Narrator 输出契约与校验回退：模型只能提供正文、选项表现、NPC台词和气氛标签，不能控制概率、Roll、Tier、奖励、关系、物品或世界阶段。
- 新增 Outcome Engine 连接层，在保留前三阶段算法的前提下，把事件结果写回线程认知/紧迫度、失败后续局面、英雄关系记忆及旅途日志。
- 新增最多保留50条的 `StateChangeLog`，覆盖旅行选取、事件结算、恢复、线程介入、关注与对话，并记录逐字段前后差异。
- 人物页升级为完整闭环 Debug，可统一检查 WorldThread、Director、Selector、Check、Outcome、AI 输入/输出校验和状态变更日志。
- 完成36次探索分布与三种代表构筑各1000次概率校准；18个模板、10种意图均被覆盖，显示72%的路线实测通过率为72.2%—74.5%。

## 0.3.5 — 2026-08-16

- 新增程序驱动的 Event Director，将原有20个基础事件改为可重复组合的地点候选池，而非按地点直接随机抽取固定事件。
- 建立环境、世界线程、玩家个人、英雄/特殊四类事件池，并加入 rumor、clue、encounter、aftermath、environmental、npc_report、hero_overlap、investigation、conflict、opportunity 与 personal 意图。
- 实装地点相关性、线程阶段、紧迫度、Tension、Recent History、玩家关注、世界相关性、Narrative Budget 与 0.85—1.15 可控扰动的完整权重公式及 Seed 抽取。
- Director只读取 WorldThread，不改变其推进职责；线程阶段、世界效果与结算后的后续钩子会作为结构化现场约束传给本地叙事和 AI Narrator。
- 每次事件使用独立 occurrence ID，固定事件模板可以在不同阶段、地点和意图下重新承担不同叙事功能，同时保持检定与刷新防重骰。
- 新增旅途日志与“关注此事”功能，关注提供25%相关权重提升但不会垄断事件池；人物页加入 Tension、候选权重、修正项、抽取结果和近期历史 Debug。
- 新增连续30次探索分布测试，覆盖四类事件交错、至少10个基础模板、至少6种意图、重复抑制、稳定 Seed、关注增权和 WorldThread 只读边界。

## 0.3.4 — 2026-08-13

- 新增独立持久化的 `WorldState` 与程序驱动的 `WorldThread` 推进器，世界时间随有效行动前进，不依赖玩家抽中相关事件。
- 加入“诺克萨斯残军活动”和“灵体异常”两条 0—6 阶段测试线程，并分别定义自然结果、介入结果、世界效果和后续剧情钩子。
- 将 Progress、Urgency、Awareness 与 Intervention Window 分离；重大不可逆阶段前若认知不足，会先停在公平性门槛并强制提供清晰征兆。
- 新增不占完整事件槽的世界征兆展示，以及调查、主动介入接口；玩家可以忽视、延迟介入或改变线程默认走向。
- 人物档案增加玩家可见的世界动态和完整 World Thread Debug 面板，显示世界时间、线程阶段、紧迫度、认知度、窗口和结算状态。
- 覆盖完全忽视、认知与进度不同步、公平预警、主动介入、延迟介入、自然结算和后续钩子测试。

## 0.3.3 — 2026-08-13

- 修复小屏幕和桌面固定高度窗口中出生页无法滚动、导致“踏入世界”按钮不可见的问题。
- 恢复地图页原有“选择去处”、恢复入口和四地点列表布局，缩放只发生在地图窗口内部。
- 地图窗口增加明确的全境/帕拉斯缩放按钮和窗口内标题，不再替换整个地图页面内容。

## 0.3.2 — 2026-08-13

- 游戏地图默认显示缩小后的艾欧尼亚全境，并将整块区域标注为“艾欧尼亚大陆”。
- 接入世界观数据库的官方地点锚点和剧情推定范围；除帕拉斯外统一以灰色显示为暂未开放。
- 增加地图放大与缩小控制；点击帕拉斯后进入地区级地图，单独展示帕拉斯、断风森林、战争遗迹与山间寺庙四个章节地点。
- 帕拉斯地区地图支持直接点击四地点进行旅行，并保留当前位置、行动消耗与恢复入口。

## 0.3.1 — 2026-08-13

- 将人物数据分为核心能力、人格倾向、命运倾向、身体状况、临时状态、长期特质、关系、线索与旧战斗兼容层。
- 重新映射开局问卷，使人格、能力小幅倾向与命运倾向分别生成，检定不再把人格当作能力加成。
- 新增良好、轻伤、受伤、重伤、濒危五级伤势及实际检定修正，并保留 HP 兼容映射。
- 在安全地点帕拉斯增加确定性休息入口，解除疲惫/紧张并改善一级伤势，返回行动、资源、金钱和未来世界时间成本接口。
- 普通失败不再导致死亡；严重战斗失败优先进入被俘状态，只有明确标记致命且前台预警的选项能够死亡。
- 从玩家可见界面彻底移除旧生命、攻击、防御、法强、魔抗、攻速、急速与战斗经验，包括顶部、人物页、事件结算、Boss 卡片和物品卡片。
- 物品改为叙事效果、线索用途与六项核心能力的情境检定帮助，旧存档加载时自动迁移。
- 游戏页右上角显示当前版本号。
- 启动器校验 `/health` 返回的版本号，并附加版本查询参数，避免旧服务或浏览器缓存冒充当前版本。
- 旧存档加载时自动补齐新结构并保留原 HP、物品、关系、事件和地图进度。

## 0.3.0 — 2026-08-13

- 新增统一 Check Engine、规定概率曲线、5%—95% 限制与四档结果。
- 使用事件 Seed、选项 ID 与玩家状态版本生成稳定判定，刷新无法重骰。
- 前台启用武艺、体魄、灵觉、心志、机敏、交涉六项核心属性，旧战斗字段降为兼容数据。
- 14 个事件配置不同属性路线、难度与情境修正，战斗也改用同一检定模块。
- 接入状态、线索、关系、装备修正接口，并显示概率来源及完整调试数据。
- 移除事件类型权重抽取，保持阶段一聚焦单个事件选择体验。

本项目使用语义化版本号：`主版本.功能版本.修复版本`。

## 0.2.0 — 2026-08-13

- 重构事件生成体验：游戏逻辑先判定，AI 负责叙事表现。
- 增加段落级流式正文、世界观过渡和统一事件状态。
- 防止重复请求、旧请求覆盖及地点切换后的流冲突。
- 恢复数据库与 AI 节点管理器启动入口。
- 建立 Git 提交、版本标签、本地备份和恢复机制。

## 后续版本规则

- 小修复：`0.2.0 → 0.2.1`
- 新功能：`0.2.0 → 0.3.0`
- 大规模且不向后兼容的改造：`0.x → 1.0.0`

每次完成更新后，必须同步修改 `VERSION` 和本文件，并创建对应 Git 标签，例如 `v0.2.0`。
