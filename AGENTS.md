# 项目更新与版本规则

任何修改本项目文件的任务，在交付前必须执行以下规则：

1. 先读取根目录 `VERSION` 和 `CHANGELOG.md`。
2. 修改前确认 Git 工作区状态，不覆盖用户未提交的改动。
3. 完成修改并通过相关测试后提升版本号：
   - 修复或小调整提升补丁号，例如 `0.2.0 → 0.2.1`。
   - 新功能提升次版本号，例如 `0.2.0 → 0.3.0`。
   - 不兼容的大改造提升主版本号。
4. 同步更新根目录 `VERSION`、`CHANGELOG.md`、`frontend/package.json`；后端版本由 `VERSION` 自动读取。
5. 为该版本创建 Git 提交和带注释标签 `vX.Y.Z`。
6. 在项目同级 `版本备份` 目录创建包含全部 Git 引用的 `.bundle` 离线备份。
7. 配置了 `origin` 时推送分支和标签；若无法推送，必须明确告知用户，本地版本仍需保留。
8. 不得提交 `.env`、SQLite 数据库、API 密钥、日志、依赖目录或构建缓存。

恢复旧版本时必须创建 `restore/vX.Y.Z` 分支，不使用破坏性的硬重置，并保留当前新版历史。
