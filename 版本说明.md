# 版本与回退说明

当前版本见项目根目录的 `VERSION` 文件。

## 安全边界

Git 和 GitHub 保存程序源码、配置模板、世界观资料与文档，但不会保存：

- `backend/database/*.db`：本地存档及 API 密钥数据库。
- `.env`：本地环境变量和密钥。
- `frontend/node_modules`、`frontend/dist`：可重新安装或构建的产物。
- 日志、进程状态和 Python 缓存。

因此，回退程序版本不会覆盖当前存档和 API 节点密钥。

## 创建新版本

完成一次代码修改后，双击 `一键发布到GitHub和Vercel.cmd`，填写一句中文更新说明并输入 `Y` 确认。版本号会自动提升，不需要手工填写。脚本会：

1. 检查工作区，并阻止密钥、数据库、日志、依赖和缓存被上传。
2. 自动执行后端测试和前端正式打包；任何一步失败都会停止发布。
3. 自动提升补丁版本并同步 `VERSION`、前后端版本、README 和更新日志。
4. 创建 Git 提交和不可混淆的版本标签。
5. 在项目同级的 `版本备份` 目录生成 Git bundle 离线备份。
6. 推送 GitHub 主分支和版本标签，随后由 Vercel 自动部署。

原来的 `创建新版本.cmd` 仍可使用，它与新入口调用同一套发布流程。

## 恢复旧版本

双击 `恢复旧版本.cmd`，从版本列表中输入例如 `v0.2.0`。脚本会新建一个 `restore/v0.2.0` 分支并切换过去，不会删除新版历史，也不会覆盖数据库。

恢复前如果存在尚未提交的源码修改，脚本会停止并提示先创建版本，防止丢失工作。

## 独立试玩历史版本

如果只是想对比旧版而不改变当前开发目录，双击 `历史版本试玩器.cmd`，再输入版本序号或完整标签，例如 `v0.7.0`。程序会：

1. 将历史代码释放到项目同级的 `history-player` 独立目录；
2. 第一次运行时构建该版本网页，并复制一份本地数据库供历史版本独立使用；
3. 在 `http://127.0.0.1:8100` 启动，不占用当前版本的 8000 端口；
4. 保持当前 Git 分支、源码、正式存档和服务器不变。

切换另一个历史版本时再次运行试玩器即可。结束后双击 `停止历史版本试玩.cmd`；已准备的历史版本和它的独立存档会继续保留。
