# 版本与回退说明

当前版本见项目根目录的 `VERSION` 文件。

## 安全边界

Git 和 GitHub 保存程序源码、配置模板、世界观资料与文档，但不会保存：

- `backend/database/*.db`：本地存档及 API 密钥数据库。
- `.env`：本地环境变量和密钥。
- `frontend/node_modules`、`frontend/dist`：可重新安装或构建的产物。
- 日志、进程状态和 Python 缓存。

因此，回退程序版本不会覆盖当前存档和 API 节点密钥。

## 创建新版本

完成并验证一次更新后，双击 `创建新版本.cmd`，按提示输入版本号和更新说明。脚本会：

1. 检查版本号格式和工作区状态。
2. 更新 `VERSION`。
3. 创建 Git 提交。
4. 创建不可混淆的版本标签。
5. 在项目同级的 `版本备份` 目录生成 Git bundle 离线备份。
6. 如果已经连接 GitHub，则推送当前分支和版本标签。

## 恢复旧版本

双击 `恢复旧版本.cmd`，从版本列表中输入例如 `v0.2.0`。脚本会新建一个 `restore/v0.2.0` 分支并切换过去，不会删除新版历史，也不会覆盖数据库。

恢复前如果存在尚未提交的源码修改，脚本会停止并提示先创建版本，防止丢失工作。
